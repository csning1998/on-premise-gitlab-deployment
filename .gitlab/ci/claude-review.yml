---
claude-code-review:
  stage: review
  image: golang:1.26-alpine
  needs:
    - job: terraform-fmt
      optional: true
    - job: packer-fmt
      optional: true
    - job: go-fmt
      optional: true
    - job: go-build
      optional: true
  variables:
    ANTHROPIC_MODEL: "claude-sonnet-4-6"
    GOTOOLCHAIN: "local"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual
      allow_failure: true
  interruptible: true
  script:
    - cd tools/ci
    - go run ./cmd/claude-review
