---
all:
  vars:
    ansible_ssh_user: "${ansible_ssh_user}"

  children:
    nodes:
      hosts:
%{ for node in harbor_nodes ~}
        ${node.key}:
          microk8s_advertise_ip: ${node.ip}
%{ endfor ~}
