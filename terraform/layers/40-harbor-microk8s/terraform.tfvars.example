
service_catalog_name = "harbor"

harbor_microk8s_config = {
  "frontend" = {
    role            = "microk8s"
    network_tier    = "default"
    base_image_path = "../../../packer/output/03-base-microk8s/ubuntu-server-24-03-base-microk8s.qcow2"
    nodes = {
      "00" = { ip_suffix = 200, vcpu = 4, ram_size = 4096, os_disk_capacity_gib = 40 }
      # Uncomment the following lines for HA setup, abide by node % 2 != 0.
      # "01" = { ip_suffix = 201, vcpu = 4, ram_size = 4096, os_disk_capacity_gib = 40 }
      # "02" = { ip_suffix = 202, vcpu = 4, ram_size = 4096, os_disk_capacity_gib = 40 }
    }
  }
}

ansible_files = {
  inventory_template_file = "inventory-microk8s-cluster.yaml.tftpl"
  playbook_file           = "30-provision-microk8s.yaml"
}
