
harbor_minio_tenants = {
  "harbor-registry" = {
    user_name      = "harbor-user"
    enable_version = false
    policy_rw      = true
  }
}

db_init_config = {
  db_name = "registry"
  db_user = "harbor"
}
