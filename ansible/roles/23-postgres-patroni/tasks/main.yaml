---
- name: "Section A: Prepare Python Environment and Install Patroni"
  block:
    - name: "Step A1. Create Python virtual environment for Patroni"
      ansible.builtin.command:
        cmd: python3 -m venv /opt/patroni-venv
        creates: /opt/patroni-venv/bin/activate

    - name: "Step A2. Install Patroni and dependencies"
      ansible.builtin.pip:
        name:
          - patroni[etcd3]
          - psycopg2-binary
        virtualenv: /opt/patroni-venv
        virtualenv_command: /usr/bin/python3 -m venv

- name: "Section B: Stop and Disable default PostgreSQL service"
  block:
    - name: "Step B1. Ensure default postgresql service is stopped"
      ansible.builtin.systemd:
        name: postgresql
        state: stopped
        enabled: false
      # Ignore errors if the service doesn't exist (it might be postgresql@.service)
      ignore_errors: true

- name: "Section C: Configure Network Routing"
  become: true
  when: postgres_static_routes is defined and postgres_static_routes | length > 0
  block:
    - name: "Step C1. Deploy Routing Fix Service"
      ansible.builtin.template:
        src: fix-routes.service.j2
        dest: /etc/systemd/system/fix-vault-routes.service
        owner: root
        group: root
        mode: "0644"
      notify: Restart Route Service

    - name: "Step C2. Enable and Start Routing Fix"
      ansible.builtin.systemd:
        name: fix-vault-routes.service
        state: started
        enabled: true
        daemon_reload: true

- name: "Section D: Configure TLS Certificates"
  become: true
  block:
    - name: "Step D0. Ensure Vault Binary is installed"
      ansible.builtin.include_role:
        name: 07-base-vault
      vars:
        vault_install_only: true

    - name: "Step D1. Create Vault Agent Config Directory"
      ansible.builtin.file:
        path: "/etc/vault.d/approle"
        state: directory
        owner: vault
        group: vault
        mode: "0750"

    - name: "Step D2. Write AppRole Credentials"
      ansible.builtin.copy:
        content: "{{ item.content }}"
        dest: "/etc/vault.d/approle/{{ item.file }}"
        owner: vault
        group: vault
        mode: "0600"
      loop:
        - { file: "role_id", content: "{{ vault_agent_role_id }}" }
        - { file: "secret_id", content: "{{ vault_agent_secret_id }}" }
      no_log: true
      notify: Restart Vault Agent

    - name: "Step D3. Deploy Vault CA Certificate (Trust Anchor)"
      ansible.builtin.copy:
        content: "{{ vault_ca_cert_b64 | b64decode }}"
        dest: "/etc/vault.d/vault-ca.crt"
        owner: vault
        group: vault
        mode: "0644"

    - name: "Step D4. Ensure Postgres Cert Directory Exists"
      ansible.builtin.file:
        path: /etc/postgresql/certs
        state: directory
        owner: postgres
        group: postgres
        mode: "0750"

    - name: "Step D5. Configure Vault Agent (vault-agent.hcl)"
      ansible.builtin.template:
        src: vault-agent.hcl.j2
        dest: /etc/vault.d/vault-agent.hcl
        owner: vault
        group: vault
        mode: "0640"
      notify: Restart Vault Agent

    - name: "Step D6. Configure Vault Agent Service"
      ansible.builtin.template:
        src: vault-agent.service.j2
        dest: /etc/systemd/system/vault-agent.service
        owner: root
        group: root
        mode: "0644"
      notify: Restart Vault Agent

    - name: "Step D7. Enable and Start Vault Agent"
      ansible.builtin.systemd:
        name: vault-agent
        state: started
        enabled: true
        daemon_reload: true

    - name: "Step D7.1. Flush handlers to restart Vault Agent immediately if config changed"
      ansible.builtin.meta: flush_handlers

    - name: "Step D8. Wait for Certificates to be generated"
      ansible.builtin.wait_for:
        path: /etc/postgresql/certs/server.crt
        timeout: 120
        msg: "Timeout waiting for Vault Agent to generate certificates."

- name: "Section E: Configure Patroni Service"
  block:
    - name: "Step E1. Create Patroni config file"
      ansible.builtin.template:
        src: patroni.yml.j2
        dest: /etc/patroni.yml
        owner: postgres
        group: postgres
        mode: "0600"
      notify: Restart Patroni

    - name: "Step E2. Create Patroni systemd service file"
      ansible.builtin.template:
        src: patroni.service.j2
        dest: /etc/systemd/system/patroni.service
        owner: root
        group: root
        mode: "0644"
      notify: Restart Patroni

- name: "Section F: Start and Enable Patroni Service"
  block:
    - name: "Step F1. Check if UFW is installed"
      ansible.builtin.stat:
        path: /usr/sbin/ufw
      register: patroni_ufw_stat

    - name: "Step F2. Allow PostgreSQL and Patroni ports"
      community.general.ufw:
        rule: allow
        port: "{{ item }}"
        proto: tcp
        src: "{{ postgres_mtls_node_subnet }}"
      loop:
        - "{{ postgres_port }}"
        - "{{ patroni_restapi_port }}"
      when: patroni_ufw_stat.stat.exists

    - name: "Step F3. Reload systemd and start Patroni"
      ansible.builtin.systemd:
        name: patroni
        state: started
        enabled: true
        daemon_reload: true
