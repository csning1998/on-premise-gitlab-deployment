---
# MinIO Service Configuration
minio_user: "minio-user"
minio_group: "minio-user"

# Disk Configuration corresponding to Terraform-mounted vdb, vdc
minio_data_drives:
  - device: "/dev/vdb"
    mount_point: "/data1"
  - device: "/dev/vdc"
    mount_point: "/data2"

# Verification TTL
vault_agent_cert_ttl: "2m"
