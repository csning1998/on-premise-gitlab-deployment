---
# Section E: Start Services
- name: "E1. Enable and start Redis services"
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: started
    enabled: true
    daemon_reload: true
  loop:
    - redis-server
    - redis-sentinel
